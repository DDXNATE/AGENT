# Agent Pippy - Environment Configuration
# ========================================
# Copy this to .env for local development
# 
# SECURITY RULES:
# 1. Variables starting with VITE_ are exposed to browser (PUBLIC)
# 2. Variables without VITE_ are backend-only (PRIVATE)
# 3. NEVER commit .env file with real keys to git
# 4. Use .env.example to document required variables
#
# Get your API keys from:
# - GEMINI_API_KEY: https://aistudio.google.com/apikey
# - GROQ_API_KEY: https://console.groq.com/keys
# - FINNHUB_API_KEY: https://finnhub.io/
# - ALPHA_VANTAGE_API_KEY: https://www.alphavantage.co/support/#api-key

# Backend API Keys (Backend-only - NEVER exposed to browser)
# Used in server.js via process.env.GEMINI_API_KEY etc.
GEMINI_API_KEY=your_gemini_key_here
GROQ_API_KEY=your_groq_key_here
FINNHUB_API_KEY=your_finnhub_key_here
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key_here

# Frontend Configuration (PUBLIC - Exposed to browser)
# Only non-sensitive values with VITE_ prefix
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
